#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
    // ios；
    // wios;
    // wcout.imbue(locale("chinese"));
    cout << "hello..." << endl;
    cout << "hello..." << flush;
    flush(cout);锁

    while (true)
    {
        /* code */
    }
    
    return 0;
}
